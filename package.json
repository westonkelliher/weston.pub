{
  "name": "weston-pub",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "CF=1 astro build",
    "preview": "CF=1 npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.7",
    "astro": "^6.1.4"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
